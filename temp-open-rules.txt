rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY PERMISSIVE RULES - FOR INITIAL DATA CREATION ONLY
    // Replace with secure rules after data is created
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}