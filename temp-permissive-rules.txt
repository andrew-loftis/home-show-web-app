rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY PERMISSIVE RULES - FOR SETUP ONLY
    // Replace with secure rules after data creation
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}